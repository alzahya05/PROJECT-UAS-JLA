# Plan: Simplify Navbar — 4 Items, No Dropdown Children

## Current State
Navbar has 4 items (Edukasi, Aksesibilitas, Komunitas, Pengaturan) each with `children` arrays in `NAV_ITEMS`. These children are not rendered anywhere in the current navbar — they're unused. The language switcher dropdown already works.

## Change
Remove `children` from `NAV_ITEMS` in `lib/constants.ts`. That's it — the Navbar already renders direct links without dropdowns. The `children` arrays are dead code.

### Files to Modify

**`lib/constants.ts`** — Remove `children` arrays from all 4 `NAV_ITEMS` entries. Keep `label`, `href`, `icon`.

That's the only change needed. The Navbar component already renders `NAV_ITEMS` as flat links, not dropdowns.

## Verification
- `pnpm build` passes
- Navbar shows 4 translated items as direct links + language switcher
- Mobile nav shows same 4 items + language selector buttons
